#include "../include/Router.h"
#include <iostream>
#include <string>
using namespace std;

int Router::InitRouter(string conn) {
    cout << conn;
    return true;
}

int Router::StartRouting() {
    cout << "start routing";
    return 0;
}

Router::Router() {

}